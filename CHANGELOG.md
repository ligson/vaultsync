# 变更记录

所有有意义的项目变更都应记录在这里。

## 2026-08-04

- 准备发布 `v1.0.0+2026080412`：包含文件名编码异常保护、相册备份上传后自动批量清理、后台/锁屏后回到前台继续处理，以及对应的跨平台客户端测试。发布和 NAS 客户端替换仅覆盖版本制品，部署前备份数据库、配置和旧安装包，不清理用户数据、同步任务、目录绑定、密文文件或加密密钥。
- 相册备份的“上传后删除”改为上传确认后自动批量清理：普通目录继续单文件清理，相册资源在整轮上传完成并持久化后统一调用系统批量删除接口；Android/iOS 锁屏或后台期间不弹删除窗口，回到 App 前台后自动继续。历史 `cleanup_pending` 相册任务改为一次确认全部处理，不再逐项勾选或限制每次 10 个；系统部分成功时逐项记录已删除和待处理状态，服务器备份不受影响。扫描/上传发现文件名含 `�` 时禁止本地自动删除并提示先重命名，文件列表改为显示“名称编码异常”，避免乱码继续干扰浏览或导致原始名称不可恢复。本次仅扩展客户端清理接口和状态处理，不迁移或清空服务器数据、SQLite、上传队列、目录绑定、同步历史或密钥。详细规则见 `docs/specs/2026-08-04-media-cleanup-and-filename-safety.md`。
- 首页列表视图精简为文件/文件夹图标、名称和同步状态图标，不再重复显示大小、日期或“服务器已备份”文字；详细视图继续保留名称、路径、大小、时间、状态和操作列。图标视图为当前设备仍存在的普通图片显示受限解码尺寸的本地缩略图，远端密文或已清理本地副本继续使用类型图标，不批量下载原文件。文件菜单新增类似系统属性窗口的“详细信息”，分组展示路径、字节大小、同步/加密状态、对象/版本/任务 ID、哈希、上传进度和错误。本次仅使用已有元数据和本地图片，不修改服务器对象、SQLite、上传队列、同步历史、目录绑定或加密密钥。
- NAS 客户端下载补齐 `1.0.0+2026080410`：Android 正式签名 APK、iOS 未签名 IPA、macOS Apple Silicon/Intel 未签名 zip 和 Windows x64 未签名 zip 分别使用真实扩展名发布；下载首页移除当前不存在的 Linux AppImage 入口，并明确 Apple/Windows 未签名限制。后端下载列表新增 iOS 和 macOS 架构项，管理后台同步支持对应文件类型。部署前备份 SQLite 和旧 Android APK，不修改用户、设备、同步目录、上传任务、密文对象或加密密钥。
- Android 发布工作流增加生产签名证书指纹校验，固定使用当前正式 APK 的公钥 SHA-256 `5e0dcce66a22640d...42ff20`；GitHub Secret 换用其他 keystore、alias 或密码错误时直接失败，不会生成被 Android 视为另一个应用的 APK。原始本地 `.jks` 仍是唯一签名源，GitHub 只保存受控副本。
- 首页文件浏览由可展开文件树改为当前目录导航：顶部显示面包屑和返回上级按钮，文件夹点击进入下一级，搜索结果仍支持全局文件名/路径搜索；列表、详细信息、图标视图、预览、下载、删除、排序和分批显示功能保持不变。每次只构建当前目录的直接子项，降低深层目录和大文件列表的 Widget 数量。同步状态页的失败任务、待清理任务、同步问题和相册清理列表改为每页最多 40 项，任务总数和页码可见，原有重试、忽略、冲突处理和清理操作保持不变。本次不修改同步任务、服务器文件、本地目录绑定或加密密钥。
- 已构建并发布首页文件视图改造版 Android APK `1.0.0+2026080409`：本地、NAS 下载目录和公网下载地址的 APK 大小均为 `76125387` 字节，SHA-256 均为 `af872fe8c4fd2420d7b27bb31feb608ba4924060f27ea8559038eefb4964b29a`；NAS 版本接口已更新，后端与前端容器保持原镜像运行。发布前已将 SQLite、配置、Compose 和旧 APK 备份到 `data/backups/app-preview-20260804-153324-2026080409/`，备份与发布后 `PRAGMA quick_check` 均为 `ok`。Pixel 7 Pro 已使用 `adb install -r --no-streaming` 保留数据覆盖安装，`versionCode=2026080409`、`firstInstallTime=2026-08-02 08:10:50` 未变化，签名证书与旧版本一致，照片/视频、网络和所有文件访问权限保持原状态；手机当前 Clash VPN 没有可用默认网络，导致安装后在线主页验证需在手机恢复网络后重试。本次不清空客户端登录态、目录绑定、上传队列、相册记录或加密密钥，也未重启 NAS 服务。
- 首页文件树增加列表、详细信息、图标三种视图切换，详细视图适配 Mac/Windows/Linux 文件管理习惯，图标视图保留目录展开和操作菜单；三种视图共用预览、下载、删除、同步状态和目录操作，并新增文件名/路径搜索及名称、修改时间、大小、同步状态排序。下载复用安全下载解密链路，桌面端使用系统“另存为”、Android 选择目录、iOS 保存到 Files 可见的 `VaultSync Downloads`；移动端同名文件自动增加后缀且不覆盖，操作期间显示明确进度。本次不修改服务端对象、上传队列、同步历史或加密密钥，只有用户明确下载时才新增本地明文文件。
- 修复“上传后删除本地文件”不生效：每个文件经服务器确认后立即按当前目录策略清理并分别落盘，不再等待上万条任务整轮结束；目录从“保留本地”改为“上传后删除”时会提示并处理旧 `clean` 任务，升级前已切换策略的旧任务也显示为待清理。删除前继续校验文件大小和修改时间，内容变化、权限失败和相册资源不会被强制删除；已不存在且确认上传完成的文件记录为本地已删除，避免永久重试。
- 上传队列从受 macOS `NSUserDefaults` 单项约 4 MB 限制的 `SharedPreferences` 非破坏性迁移到应用支持目录的 32 个原子 JSON 分片；单任务进度只更新一个分片，编解码在后台 isolate 执行，兼容 Android、iOS、macOS、Windows 和 Linux。首次迁移会合并旧任务与可恢复分片，成功后才启用新清单，并保留旧偏好键作为迁移备份；不清空目录绑定、上传队列、相册记录、同步历史、密文或加密密钥。
- 修复本地文件在上传前或重试前已被用户删除时，失败任务永久滞留的问题：上传执行器仅在系统确认异常路径就是当前源文件且错误为“路径不存在”时，将对应未完成任务移出本地队列；手动和自动上传会显示移除数量。权限、网络、服务端、临时缓存和相册资源错误仍保留为失败任务。本次不删除服务器已完成版本或未完成密文分片，不修改同步目录绑定、历史、远端索引和加密密钥，任务 JSON 与后端协议保持兼容。
- tag `v1.0.0+2026080408` 已完成含 Windows 的 GitHub Release 全链路验证：Windows x64 无签名 zip 在 Visual Studio 2022 下编译、打包、解压回验和 artifact 上传全部成功，发布包包含 `vaultsync_app.exe`、Flutter/插件 DLL、PDFium 和完整 `data/flutter_assets`；下载后 SHA-256 `efb84c4c534f9f52db5c4655740190fa31606669c936340ee4ed0d66321a4b06` 与 Release 清单一致。完整流水线所有 job 成功，Release 共包含 11 个构建制品及 `SHA256SUMS.txt`。本次未安装 Windows 客户端、未部署 NAS，也未修改服务端或客户端数据。
- tag `v1.0.0+2026080407` 首次执行 Windows 原生构建，确认签名模式和 Flutter 环境准备正常，但 `windows-2025` 的 Visual Studio 18 将 `permission_handler_windows 0.2.1` 使用的 experimental coroutine 弃用提示升级为编译错误，因此未发布不完整 Release；Windows job 改用官方 `windows-2022` 与 Visual Studio 2022 稳定工具链，不通过全局关闭编译器保护掩盖依赖问题，也不调整应用依赖版本。
- GitHub tag 发版新增 Windows x64 客户端：使用 `windows-2022` runner 构建并将 EXE、Flutter 运行库、插件 DLL 和 data 目录完整打包；未配置 PFX 时发布 `windows-x64-unsigned.zip` 并提示 SmartScreen 风险，完整配置两项 Windows Secret 后自动对主 EXE 做 Authenticode SHA-256 签名、可信时间戳和验签，部分配置则拒绝发布。Windows 制品已纳入统一 Release、11 个构建文件数量校验和 SHA-256 清单。本次仅调整 CI 与文档，不修改 NAS、SQLite、服务端对象、客户端本地状态或同步数据。
- tag `v1.0.0+2026080406` 已完成无 Apple Developer 证书的 GitHub Release 全链路验证：基础测试、Android 正式签名 APK/AAB、iOS `unsigned.ipa`、macOS x64/arm64 `unsigned.zip`、前端 zip、Linux/macOS amd64/arm64 后端包和发布 job 全部成功。Release 共包含 10 个构建制品及 `SHA256SUMS.txt`，校验清单覆盖全部构建制品；iOS 包已验证 `Payload/Runner.app` 结构。此次仅发布 GitHub 制品，未安装手机、未部署 NAS，也未修改服务端数据或客户端本地状态。
- tag `v1.0.0+2026080405` 首次进入 iOS 无签名构建分支，但 `macos-15` runner 的旧 iOS SDK 无法编译 `device_info_plus 13.2.0` 使用的 `NSProcessInfo.isiOSAppOnVision`，因此未发布不完整 Release；iOS job 改用官方 `macos-26` arm64 runner，以匹配当前 Flutter 插件所需 SDK。该修复不调整依赖版本、业务代码或客户端数据格式。
- GitHub tag 发版工作流支持无 Apple Developer 账号场景：iOS 在 4 项签名 Secret 全部缺失时生成明确标注 `ios-unsigned.ipa` 的待重签名包，macOS 在 6 项签名/公证 Secret 全部缺失时生成 x64、arm64 的 `unsigned.zip`；任一平台只配置部分凭据时仍直接失败，完整配置后自动恢复正式签名与公证。Android 继续强制使用现有正式 keystore。Release 说明同步提示 iOS 不可直接安装、macOS 可能受 Gatekeeper 限制。本次仅调整 CI 和文档，不修改 NAS 数据、SQLite、服务端对象、客户端同步映射、上传队列或加密密钥。
- tag `v1.0.0+2026080404` 已验证 Android GitHub Actions 全链路：正式 keystore 导入、APK/AAB release 编译、APK `apksigner` 验签、AAB `jarsigner` 验签和 artifact 上传全部成功；`vaultsync-app-android` artifact 大小为 93,317,878 字节，artifact digest 为 `sha256:4d8ebc083f15e4d33756dabcc1094cf587896ce303204a3b621eef48b76947ce`。本轮前端 zip 与 Linux/macOS amd64/arm64 后端制品也全部成功；iOS 和 macOS 因仍缺少 Apple Developer 签名材料失败，Publish job 按设计跳过，未生成不完整 GitHub Release。本次不安装 APK、不部署 NAS，也不修改 SQLite、服务端对象或客户端本地数据。
- 已将本机当前 Android 正式 keystore 的 4 项配置安全写入仓库 GitHub Actions Secrets，未在日志或仓库中暴露签名值；`v1.0.0+2026080403` attempt 2 已通过签名准备并成功编译 APK/AAB，随后发现 `jarsigner -verify -strict` 会把 AAB 内嵌依赖的 JarFile/JarInputStream 签名差异告警当成 exit 4。工作流改用标准 `jarsigner -verify -verbose -certs`，并额外强制检查 `jar verified.`，既避免该误判，也继续拒绝未签名 AAB。iOS/macOS 仍因缺少 Apple Developer 材料保持失败，不生成不完整 Release；本次不修改 App 签名身份、NAS 数据、SQLite、服务端对象或客户端本地数据。
- 第二次 tag 发版演练 `v1.0.0+2026080403` 已通过 tag/commit 校验、后端测试、前端构建、Flutter analyze 和 222 项 Flutter 测试，并成功生成前端 zip 与 Linux/macOS amd64/arm64 四种后端制品；Android、iOS、macOS 签名 job 因仓库尚未配置任何 GitHub Actions Secrets 而按设计失败，Publish job 被跳过，未创建不完整的 GitHub Release。本机存在当前 Android 正式 keystore，可在用户确认后安全写入 GitHub Secrets；本机没有 Apple codesigning identity 或 iOS provisioning profile，因此 iOS 签名和 macOS notarization 仍需有效 Apple Developer 材料。本次未上传任何本机签名材料，未修改 NAS 数据、SQLite、服务端对象或客户端本地数据。
- 首次 tag 发版演练保留失败 tag `v1.0.0+2026080402`：tag 与 commit 校验、后端测试和前端构建均通过，Flutter 验证因用例硬编码 Asia/Shanghai 展示时间而在 UTC runner 上出现 1 项失败，因此未进入签名构建且未创建 GitHub Release。已将断言改为按 runner 本地时区计算，并将 `subosito/flutter-action` 从 `v2.9.1` 升级到 `v2.23.0`，消除其旧 `actions/cache@v3` 的 Node.js 20 弃用警告；修复后使用新 build number 发版，不移动或复用失败 tag。本次不修改 NAS 数据、SQLite、服务端对象、客户端目录绑定、上传队列、同步历史或加密密钥。
- 新增基于 GitHub tag 的统一发版工作流：固定从 `v<semver>+<build-number>` tag 指向的 commit 构建，产出 Android 签名 APK/AAB、iOS 签名 IPA、macOS x64/arm64 签名公证包、前端 zip，以及 Linux/macOS amd64/arm64 后端 tar.gz，并在同一 GitHub Release 附带 SHA-256 校验文件。所有移动端和桌面端签名材料缺失时均直接失败，不回退到 debug 或未签名包；本次仅新增 CI 与文档，不修改 NAS 数据、SQLite、服务端对象、客户端目录绑定、上传队列、同步历史或加密密钥。
- 已发布签名 Android APK `1.0.0+2026080401` 到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，APK 大小为 75,846,383 字节，本地、NAS 和公网断点续传后的 SHA-256 均为 `ae1f69b0f5879157766361bbbf460396517361f6d67031503d98d7d988791b2f`。发布前已将 SQLite、配置、Compose 和旧 APK 备份到 `data/backups/app-release-20260804-084607-2026080401/`，备份和发布后数据库快速完整性检查均为 `ok`；本次后端无代码变化，NAS 继续运行镜像 `ligson/vaultsync-be:2026080301-e4cbfd9-session-tree`，未重建或重启服务，也未修改用户、设备、同步目录、上传会话、文件版本、密文对象或加密密钥。已使用 `adb install -r --no-streaming` 保留数据覆盖升级华为 `HUAWEI NOH-AN00`（`PQY0221120086806`），`firstInstallTime=2026-07-30 19:45:07` 未变化，登录主页、通知权限、所有文件访问权限和前台同步服务均保留；熄屏 30 秒期间进程保持运行，服务器新增 8 个文件版本并继续接收上传字节。
- 修复 Android 扫描、上传和重试大文件时的高内存与界面卡顿：普通文件改为流式哈希和按分片读取，加密文件改为流式生成与现有 `VSENC001 + XChaCha20-Poly1305` 完全一致的临时密文，不再同时把整份明文、密文和分片保留在内存；上传任务 JSON 编解码移到后台 isolate，进度持久化间隔调整为 32 MB，目录扫描和任务合并按批次让出事件循环。
- 增强锁屏与进程恢复：Android 仅在实际上传期间持有 `PARTIAL_WAKE_LOCK`，上传结束立即释放；未完成会话在 App 启动时以服务器 `received_size` 校准真实进度并自动续传，界面显示“待续传”百分比。加密临时文件通过 `.part` 原子生成，失败或进程退出后可复用，上传成功后才清理。
- 本次不修改后端协议、SQLite、服务器对象布局或既有 `VSENC001` 密文，不清空或迁移客户端 `SharedPreferences`、同步目录绑定、上传队列、相册记录、同步历史和加密密钥。回滚旧 App 时只会忽略新增临时缓存；服务器已完成的会话仍由客户端执行远端基线登记和用户配置的本地清理收尾，避免状态提前结束。

## 2026-08-03

- 已从当前 `main` 重新构建并发布签名 Android APK `1.0.0+2026080302` 到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，APK 大小为 75,748,079 字节，本地、NAS 和公网文件 SHA-256 均为 `facb49109ec6e1952f9658b4f419a357d976c854a0e10cf7e814afea8b67ef7b`。发布前已将 SQLite、配置、Compose 和旧 APK 备份到 `data/backups/app-release-20260803-175524-2026080302/`，备份数据库完整性检查通过；本次后端无代码变化，NAS 继续运行最新镜像 `ligson/vaultsync-be:2026080301-e4cbfd9-session-tree`，未重建或重启服务，也未修改用户、设备、同步目录、上传任务、文件版本、密文对象或加密密钥。华为 `HUAWEI NOH-AN00` 在覆盖安装前断开 USB，未执行真机升级，不记录为已安装。
- 已发布并部署后端镜像 `ligson/vaultsync-be:2026080301-e4cbfd9-session-tree`（digest `sha256:3913e031e624d4085fc0643da2a0513ea7e80c0db2ad69c458919c02321542eb`），并发布签名 Android APK `1.0.0+2026080301` 到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`；APK 大小为 75,748,079 字节，本地、NAS 和公网文件 SHA-256 均为 `ed9f18ff9707da9c9e36ac6c1fba39eb5d9fc0cf0a2f3b919ed712166ebee9ea`。NAS 无法直接拉取 Docker Hub 时使用 `docker save`、`scp -O` 和 `docker load` 离线导入同名镜像。部署前已将 SQLite、配置、Compose 和旧 APK 备份到 `data/backups/app-release-20260803-155109-2026080301/`；备份及部署后数据库完整性检查均通过，6 个用户、4 台设备、6 个同步目录、14,397 条上传会话、10,446 个文件版本和 24 条会话均保留。已使用 `adb install -r --no-streaming` 保留数据覆盖升级华为 `HUAWEI NOH-AN00`（`PQY0221120086806`），`firstInstallTime=2026-07-30 19:45:07` 未变化，原文件访问和通知权限保持授权，App 主 Activity 可正常启动。
- 优化同步主页文件树的手机与桌面布局：文件名固定单行省略，大小/时间和同步状态移到稳定的副标题区域，右侧只保留操作菜单；目录层级缩进收紧并限制最大视觉缩进，普通“已同步/已备份”状态不再在每一级目录重复展示，根目录摘要隐藏重复设备信息和为零的待上传/问题标签。继续保留每批 150 项的分批渲染策略，并新增 360 像素窄屏、深层目录和超长文件名回归测试。本次仅调整展示，不修改或清理同步目录、上传队列、同步历史、服务器对象、SQLite、相册记录或加密密钥。
- 修复 App 使用约 24 小时后提示“登录状态已失效，请重新登录”的问题：访问令牌继续保持 24 小时有效，新增固定 30 天有效的 Refresh Token；服务端仅保存 SHA-256 哈希并在每次刷新时原子轮换，Flutter 客户端可在访问令牌过期后自动续期，并合并并发 401 的刷新请求以避免重复轮换和额外网络开销。刷新接口继续兼容访问令牌尚有效的旧客户端；旧客户端升级后如访问令牌已经过期，需要重新登录一次获得 Refresh Token。
- SQLite 仅为 `sessions` 非破坏性新增 `refresh_token_hash`、`refresh_expires_at`、`revoked_at` 字段和非空哈希唯一索引，现有会话记录完整保留且新增字段默认为空；本次不删除或清空用户、设备、同步目录、上传任务、同步历史、密文文件、下载文件、客户端目录绑定、相册记录或加密密钥。代码回滚时新增数据库字段可安全保留。

## 2026-08-02

- 已发布并部署后端镜像 `ligson/vaultsync-be:2026080202-5692183-device-storage`，发布签名 Android APK `1.0.0+2026080202` 到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，本地、NAS 和公网文件 SHA-256 均为 `b0430d6d98755dad67977fa0496689aa1f184552acc91ae6b16c9a9950282b24`。部署前备份 SQLite、配置、Compose 和旧 APK 到 `data/backups/app-release-20260802-162934-2026080202/`，完整性检查通过；部署后 6 个用户、10 条设备记录、6 个同步目录和既有文件版本均保留，内网与公网健康检查正常。已使用 `adb install -r --no-streaming` 保留数据升级 Pixel 7 Pro，`firstInstallTime=2026-08-02 08:10:50` 未变化，登录态、当前设备识别和照片/视频权限保留；真机“设备与空间”确认当前账号展示 9 台设备（3 台有同步目录、6 条历史空记录），未自动移除任何设备。
- 合并同步中心重复的“同步状态”和“设备与目录”为单一“同步概览”：保留总体统计、失败与清理重试、自动同步状态，并按设备集中展示每个目录的待上传、失败、问题数量和最近自动/手动扫描上传状态，当前设备继续明确标识。本次只调整展示结构，不修改或清理同步任务、目录绑定、同步历史、服务端文件和密钥。
- “我的”将“使用空间”升级为独立“设备与空间”页面，集中展示设备总数、总空间、每台设备的同步目录数量和目录级文件数/空间占用；支持用户确认移除非当前且没有同步数据的历史空设备。后端同时兼容旧客户端空 `client_key` 注册并复用最近同名同平台设备，防止继续产生重复记录。设备移除接口会拒绝当前设备及任何仍有同步目录、上传会话或删除记录引用的设备，不删除同步目录、文件版本、对象文件或客户端数据，也不会在升级时自动清理已有设备。
- 已将后端镜像 `ligson/vaultsync-be:2026080201-c01f825-sync-reliability` 部署到 nas-proxy，并发布签名 Android APK `1.0.0+2026080201` 到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，三端 SHA-256 均为 `6ab0b8fc47fb5ad3cc4359335422b63be980b76c9db5f3757eb498c67a9be341`。部署前使用 SQLite `.backup` 备份数据库、真实配置、Compose 和旧 APK 到 `data/backups/app-release-20260802-2026080201/`；备份完整性检查通过，部署后 6 个用户、10 台设备、6 个同步目录和 7408 个文件版本均保留，NAS 与公网健康检查正常。手机因 USB 调试尚未授权，本条不记录真机安装完成。
- 补齐同步可靠性与可见性：Android 使用数据同步前台服务支持锁屏和切换应用后继续处理队列；扫描、上传增加目录任务互斥与自动/手动状态记录；首页改为设备下拉筛选，同步状态按设备和目录分组并标识当前设备；空间用量改为按最新未删除对象实时统计到设备和同步目录；App 更新按当前平台查询安装包，支持相对下载地址并显示打开状态和错误。本次采用非破坏性兼容方案，只新增查询索引和本地状态键，不清空服务端数据、客户端目录绑定、上传队列、同步历史、相册记录或加密密钥。

## 2026-08-01

- 已构建签名 Android release APK `versionCode=2026080103`，使用 `adb install -r --no-streaming` 保留数据覆盖安装到华为 `HUAWEI NOH-AN00`（`PQY0221120086806`）；回查确认 `firstInstallTime=2026-07-30 19:45:07` 未变化、App 已启动、联网和原文件访问授权仍保留。NAS 发布前已将 SQLite 和旧 APK 备份到 `data/backups/app-release-20260801-230134-2026080103/`，官网最新版更新为 `1.0.0+2026080103`。本次未清空客户端上传队列、相册记录、目录绑定、同步历史、加密密钥或服务端用户数据。
- Flutter 同步拉取改为元数据优先：上传成功后立即保存本地版本基线；拉取时依次检查已记录的 `version_id`、删除本地策略、已完成上传任务和明文 SHA-256，仅在远端内容确实不同或旧对象缺少可比较元数据时下载完整文件。同步状态页新增“无需下载”计数。加密目录的明文指纹只写入加密元数据，服务器无法直接读取；普通目录写入普通元数据。本次不修改后端数据库或对象布局，不删除或重置已有服务端文件、本地目录绑定、上传队列、同步游标、冲突记录和加密密钥，旧对象继续兼容并按原下载流程处理。
- 增加 macOS、Windows、Linux 同步目录文件监控：使用目录事件监听和防抖合并，在应用运行期间自动触发现有同步流程；保留 5 分钟全量扫描作为兜底。监听不修改已有本地目录绑定、上传队列、同步历史、密文文件或加密密钥。
- 已构建 Android release APK `versionCode=2026080102`，并使用 `adb install -r --no-streaming` 保留数据覆盖安装到华为 `HUAWEI NOH-AN00`；回查确认 `firstInstallTime=2026-07-30 19:45:07` 未变化、App 可正常启动、联网和照片读取权限仍保留。本次未清空客户端上传队列、相册记录、目录绑定、同步历史或加密密钥。
- 已构建 Android release APK `versionCode=2026080101`，并使用 `adb install -r` 保留数据覆盖安装到华为 `HUAWEI NOH-AN00`（`PQY0221120086806`）；回查确认 `firstInstallTime=2026-07-30 19:45:07` 未变化、App 可正常启动、联网和原照片读取权限仍保留。本次未清空客户端上传队列、相册记录、目录绑定、同步历史或加密密钥。
- 排查华为 `HUAWEI NOH-AN00` 上传状态：手机网络和线上健康接口正常，待上传任务从 10,739 降至 5,708 且失败数保持为 0，确认上传实际正在进行，原界面缺少实时反馈是“上传不上去”的主要感知原因。
- Flutter 上传执行器新增进程内实时进度通道，同步主页和同步状态页现在展示当前文件、读取处理/连接/分片上传/服务端确认阶段、单文件字节进度、本轮任务序号以及完成/失败/剩余数量；一轮上传结束后同步状态页会自动刷新统计。原有每 8 MB 持久化断点进度策略保持不变，本次不修改或清理上传队列、相册记录、目录绑定、同步历史、加密密钥、SQLite 或服务端对象。
- 同步中心进度面板移除出同步首页，仅在“同步状态”页展示；新增上传分片速度和下载单文件平均速度，并显示下载阶段、当前远端文件、字节进度和任务统计。本次不修改同步协议、服务端对象或本地数据格式。

## 2026-07-31

- 已发布 Android release APK `versionCode=2026073111`：使用 `adb install -r` 保留数据升级设备 `SM02G4061910351`，回查确认 `firstInstallTime` 未变化且相册图片/视频权限仍保留；NAS 旧 APK 和 SQLite 已备份到 `data/backups/apk-release-20260731-1630/`，官网最新版与版本接口已更新为 `1.0.0+2026073111`，NAS 文件 SHA-256 与本地构建一致。
- 已发布并部署兼容“我的”页面的后端镜像 `ligson/vaultsync-be:2026073111-profile`。部署前备份 SQLite、`config.yaml` 和 `docker-compose.yml` 到 `data/backups/backend-release-20260731-1625-profile/`；数据库仅追加 `username`、`nickname` 字段和非空用户名唯一索引，部署后 6 个用户、9 台设备、4 个同步目录均保留，健康接口正常。
- Flutter 客户端登录后重构为“同步 / 我的”一级导航：新增个人资料、头像（仅保存在 App 私有目录）、修改密码、空间用量、服务器设置、App 更新、关于和退出登录入口；同步页移除重复的右上角退出按钮。后端非破坏性新增普通用户资料查询/修改、校验当前密码改密和公开版本元数据接口，SQLite 仅追加 `username`、`nickname` 字段及非空用户名唯一索引，不清理服务端数据、客户端目录绑定、上传队列、同步历史或加密密钥。
- Flutter 客户端新增服务器备份在线预览：支持常见图片缩放查看、视频播放与进度拖动、PDF 翻页缩放，以及常见文本/代码文件滚动和选择；点击可预览的文件行或文件操作菜单即可进入。预览继续复用客户端下载与解密链路，视频只在系统临时目录生成播放文件并在退出时清理，同时按类型限制在线预览体积，避免大文件耗尽设备内存。本次不修改服务端协议、SQLite、对象文件、同步目录、上传队列、同步历史或加密密钥。
- 精简同步主页：不再展示“待处理问题”标题、问题明细和处理按钮，首页只负责浏览与管理同步目录；冲突、远端删除保护等问题统一在“同步状态”页面查看、批量处理或关闭提醒。问题数据和记录不会被删除，本次仅调整客户端展示入口，不修改服务端数据、SQLite、对象文件、本地目录绑定、上传队列、同步历史或加密密钥。
- 同步主页新增设备筛选：存在多台设备时默认只展示当前设备目录，可切换到“全部设备”或指定其他设备查看其只读目录；目录数量会跟随筛选范围变化，其他设备目录继续禁止扫描、上传、管理和删除操作。对于服务端仅保存路径哈希、无法安全恢复原名称的其他设备自定义目录，继续显示同步目录短 ID，避免伪造目录名或泄露本地明文路径。本次仅调整客户端展示与筛选状态，不修改服务端数据、SQLite、对象文件、客户端目录绑定、上传队列、同步历史或加密密钥。
- 已构建 Android release APK `versionCode=2026073110`，备份 NAS 原 APK 和 SQLite 后发布到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，下载记录更新为 `1.0.0+2026073110`；NAS 容器内文件 SHA-256 与本地构建一致，公网响应文件大小也已回查一致。
- 本次真机安装误用了会先卸载旧版的 `flutter install --use-application-binary`，设备 `SM02G4061910351` 的 `firstInstallTime` 和运行权限已被重置，当前回到登录页；服务端账号、同步目录、已上传文件、NAS SQLite 和对象存储未被删除，但旧客户端上传队列、相册扫描记录、本地映射和本地密钥可能无法完整恢复。已新增 Android 发布安装约定，后续真机升级必须使用 `adb install -r` 并回查数据保留状态。
- 优化同步主页刷新体验：首次加载仍显示加载态，后续扫描、上传、拉取、关闭问题或自动同步触发刷新时保留当前目录列表，只在顶部显示细进度条；刷新失败时保留旧内容并显示中文错误提示，避免像网页一样整页白屏刷新。本次仅调整客户端展示状态，不清空登录态、同步目录映射、上传队列、同步历史、服务端数据或加密密钥。
- 已重新构建 Android release APK `versionCode=2026073109`，上传到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，下载记录已更新为 `1.0.0+2026073109`，并已覆盖安装到设备 `SM02G4061910351`。本次仅替换下载包和覆盖安装 App，不清空客户端数据或服务端数据。
- 同步主页连接后端失败时，错误页新增当前后端地址展示、“重试”和“后端地址”入口，可直接修改服务器地址并测试连接，不再只能返回登录。该改动只调整客户端 UI 和本地后端地址设置，不清空登录态、同步目录映射、上传队列、服务端数据或加密密钥。
- 已重新构建 Android release APK `versionCode=2026073108`，上传到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，下载记录已更新为 `1.0.0+2026073108`，并已覆盖安装到设备 `SM02G4061910351`。本次仅替换下载包和覆盖安装 App，不清空客户端数据或服务端数据。
- 同步状态页新增下载冲突批量处理：可一次性“全部上传冲突副本”或“全部关闭冲突提醒”，两个操作都会先弹出中文确认说明；批量上传失败时会展示首个失败原因。该能力只新增本地上传任务或关闭本地提醒，不删除本地原文件、冲突副本、服务端备份、SQLite 数据、上传队列、同步历史或加密密钥。
- 已重新构建 Android release APK `versionCode=2026073107`，上传到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，下载记录已更新为 `1.0.0+2026073107`，并已覆盖安装到设备 `SM02G4061910351`。本次仅替换下载包和覆盖安装 App，不清空客户端数据或服务端数据。
- 修复手机端拉取服务器变更时报“密文格式无效”的问题：后端 `/api/v1/changes` 现在会为 upsert 变更返回 `encrypted_name`、`content_hash`、`size_bytes`、`metadata_json` 等版本元数据，避免客户端拿到空 metadata 后误按加密内容解密普通存储文件。本次仅补齐接口返回字段和回归测试，不修改已有同步事件、文件版本、对象文件、SQLite 数据或客户端本地队列。
- 优化同步状态页刷新交互：点击“刷新状态”时保留当前内容，仅在顶部显示细进度条、右上角按钮显示小加载状态；刷新失败时以内联中文提示展示，不再整页切换到空白加载态。
- 已重新构建并部署 Android release APK `versionCode=2026073106`，并同步更新到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`。同时后端已发布并部署镜像 `ligson/vaultsync-be:142f6ff`。本次只替换程序版本和下载包，不清空客户端数据或服务端数据。
- 修复后端变更拉取接口的设备过滤：`GET /api/v1/changes?device_id=...` 现在会按同步目录所属设备返回变更，避免新手机拉取到旧手机的历史同步目录、相册备份或加密文件，导致客户端提示“拉取服务器变更失败”。不传 `device_id` 的旧调用保持兼容。本次仅调整查询条件和回归测试，不删除服务端 `data/`、SQLite、对象文件，也不修改已有同步目录、上传队列、同步历史或密钥。
- 修复客户端旧上传会话复用判断：上传会话指纹新增文件名和 metadata，避免普通存储旧失败任务继续复用缺少 `relative_path` 的旧服务端会话，从而反复提示“普通存储文件缺少相对路径”。本次不清空本地上传队列，旧失败任务可通过“重试”重新创建正确会话。
- 修复服务端目录配置与手机本地旧映射不一致的问题：同步主页加载时会用服务端同步目录配置刷新本地映射，并将未完成上传任务的加密开关同步为服务端真实值；上传执行器也会在真正上传前按本地同步目录映射做最后校正。当加密开关变化时会清掉旧上传会话字段，强制后续重试创建新会话，避免普通存储目录继续按加密包上传。本次不删除本地上传队列、不清空同步历史、不修改服务端对象文件。
- 已重新构建并安装 Android release APK `versionCode=2026073105`，同时上传到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`。本次仅覆盖安装 App 和替换下载包，不清空客户端登录态、同步目录、上传队列、相册记录、加密密钥或服务端用户数据。

## 2026-07-31

- 精简 Flutter 同步主页顶部操作栏：标题固定为“同步”，仅保留“同步状态”和“更多操作”，相册备份、同步记录、扫描、上传和拉取统一收纳到带文字的菜单，避免横屏时标题被挤压，也降低首页视觉噪音。本次仅调整客户端展示和入口，不修改服务端数据、SQLite、对象文件、目录绑定、上传队列、同步历史或加密密钥。
- “我的”新增“权限与存储”页面，集中检查照片和视频权限、Android 共享文件夹的“所有文件访问”权限，并显示“已授权/部分授权/未授权/无需额外授权”状态；支持逐项发起授权、打开系统设置、返回 App 自动复查，帮助用户在上传或扫描前处理权限问题。本次不修改本地目录绑定、上传任务、相册记录或服务端数据。
- 新增跨平台权限检查抽象，Android 使用 `photo_manager` 和 `permission_handler` 读取真实状态，iOS/macOS 仅检查相册权限，其他桌面平台显示无需额外授权；新增权限页面和同步主页横屏回归测试。

## 2026-07-30

- 修复 Android 下载目录扫描体验：扫描 `/storage/emulated/0/Download` 或 `/sdcard/Download` 前会先检查“所有文件访问权限”，未授权时直接引导到系统授权页并显示中文说明，不再误报扫描到 0 个文件。自动同步遇到该问题只记录中文失败原因，不主动弹系统页。
- 优化多设备同步目录展示：其他设备的目录缺少本机路径映射时不再显示“未绑定目录”，可识别的下载目录显示为 `Download`，其余目录显示为同步目录短 ID，并标明“其他设备目录，仅可查看”。本次仅调整客户端展示与权限判断，不删除服务端 `data/`、SQLite、对象文件，也不清理客户端本地绑定、上传队列、相册记录或密钥。
- 修复卸载重装/覆盖安装后同步目录显示“未绑定目录”的恢复体验：同步主页加载当前设备目录时，会自动恢复可安全识别的本地映射。相册备份根据 `media-backup:v1:*` 自动恢复；Android 下载目录会通过服务端保存的路径 hash 匹配 `/storage/emulated/0/Download` 后自动恢复。其他自定义目录因服务端不保存明文路径，仍需用户手动重新选择，避免泄露本地路径隐私或误绑定。
- 修复 App 卸载重装后同一台手机被识别为“其他设备”的问题：客户端登录注册设备时新增 `client_key` 设备识别信息，后端在同一用户下优先按该 key 复用已有 `device_id`，避免原同步目录变成只读的其他设备目录。
- 后端 `devices` 表新增 `client_key` 字段和 `(user_id, client_key)` 非空唯一索引；旧客户端不传 `client_key` 时仍按原逻辑注册设备。新客户端第一次登录时，如果发现同一用户下存在唯一的“同名同平台且未绑定 key”的旧设备，会把 key 绑定到旧设备并复用原 `device_id`。
- Flutter 客户端基于各平台可读取的公开设备信息生成稳定 key：Android 使用品牌、型号、产品、硬件和系统构建指纹等信息；iOS/macOS/Windows/Linux 分别使用系统提供的 vendor/system/device/machine 标识。拿不到稳定信息时不发送 key，避免误合并设备。
- 本次变更不删除服务端 `data/`、不清空 SQLite、不修改已有同步目录绑定、不清理客户端本地上传队列或相册备份记录；数据库仅做非破坏性新增字段和索引。
- 修正旧 SQLite 数据库升级时的设备索引创建顺序：先为 `devices` 表补 `client_key` 字段，再创建唯一索引，避免旧库启动时报 `no such column: client_key`。发布过程中发现该问题后已先回滚线上后端恢复服务，随后补充旧库迁移回归测试。
- 设备认领策略增加线上旧数据兼容：如果同一用户下存在多个旧 Android 设备记录，且只有一台或最明确的一台旧设备拥有同步目录，新客户端登录时会优先把 `client_key` 绑定到这台旧设备，避免真实手机重装后仍被显示成“其他设备”。
- 已重新构建 Android release APK `versionCode=2026073001` 并上传到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`；本次仅替换下载包，不安装到手机、不清空客户端数据。

## 2026-07-29

- 修复上传完成后的幂等处理：当临时分片 `.part` 已被移动、但正式对象文件已经存在且大小正确时，后端会复用正式文件继续完成版本登记，避免手机端重试时反复失败。
- SQLite 连接调整为单连接并设置 `busy_timeout=5000`，降低单机 NAS 上大量相册上传、后台查询和维护工具同时访问数据库时出现 `database is locked` 的概率。
- 新增上传会话维护工具 `cmd/upload-session-repair`，支持 `dry-run` 和 `apply`，用于修复“文件版本已经存在或对象文件已经落盘，但 `upload_sessions` 仍停在 pending”的历史状态；`apply` 会先自动备份 SQLite。
- 已在 nas-proxy 执行上传会话修复：先备份数据库到 `/var/services/homes/ligson/wk/docker-services/vaultsync/data/backups/upload-session-repair-20260729-034633/vaultsync.db`，修复 477 条历史卡住会话；随后二次修复 2 条并回查确认可自动恢复记录为 0。本次不删除服务端对象文件、不清空 SQLite、不清理手机本地上传队列或相册备份记录。
- 维护工具补充并发幂等保护：如果修复过程中发现同一 `version_id` 已被其他上传流程写入，只更新上传会话为 completed，不再重复插入文件版本。

## 2026-07-28

- 修复手机端同步范围：手动“扫描本地文件”和自动同步只处理当前设备自己的同步目录，其他设备目录继续只读展示，避免当前手机误扫/误传其他设备目录并触发“同步目录不属于当前设备”或泛化失败提示。本次不清理本地上传队列、不删除服务端对象、不迁移 SQLite 数据。
- 自动同步失败提示增加阶段信息，状态页和同步记录会显示“扫描本地目录失败 / 扫描相册备份失败 / 上传待处理任务失败 / 拉取服务器变更失败”等中文信息；常见 Android 文件权限、文件不存在和插件权限异常也改为中文可读提示。
- 重新构建并发布 Android APK `versionCode=2026072901` 到 `https://files.ligson.xyz/downloads/vaultsync-android-latest.apk`，并已对当前连接手机执行覆盖安装；安装不清空客户端登录态、同步目录、上传队列、相册备份记录或加密密钥。
- 相册备份新增“服务器端加密存储”可配置项：新建相册备份时可选择加密或普通存储，扫描生成的相册上传任务会继承该配置；旧相册备份配置缺少该字段时继续默认加密，避免升级后改变既有数据保护策略。
- 新增后端存储布局迁移维护命令 `cmd/storage-layout-migrate`：支持 `dry-run`、`apply`、`cleanup` 三阶段整理旧对象；普通旧文件可迁移到可读目录镜像和隐藏历史版本，加密旧文件迁移到 `encrypted/` 分层，缺失设备信息的历史密文进入 `encrypted/__legacy_device__/`。`apply` 会先备份 SQLite 并写入迁移报告，`cleanup` 只删除不再被数据库引用且新文件校验通过的旧 `.bin` 副本。
- 更新服务端存储布局文档和后端 README，明确 NAS 宿主机目录与容器 `/data` 的迁移参数、旧对象兼容规则和清理顺序。本次工具改造不默认删除任何数据，实际清理必须在迁移和校验完成后执行。
- 使用新维护工具完成一次线上存储整理：963 个旧对象迁移到新结构并更新数据库路径，395 个普通文件生成真实目录镜像，963 个迁移后的旧 `.bin` 副本删除，另清理 5 个不再被数据库引用的旧层级文件；8 个大小与数据库记录不一致的旧对象保留原路径待后续排查。已生成 SQLite 备份与迁移报告，后端健康检查通过。
- 调整服务端对象存储布局：新上传的普通存储文件按 `plain/{device_id}/{sync_root_id}/{原始相对路径}` 生成可读 current 镜像，历史版本保存在隐藏 `.vaultsync_versions/{object_id}/{version_id}/{原文件名}`；新上传的加密文件按 `encrypted/{device_id}/{sync_root_id}/{version_id}.bin` 保存密文。旧 `.bin` 对象继续保留并由数据库精确引用，本次不删除、不迁移、不重写既有数据。
- 更新 `docs/notes/server-storage-layout.md`，明确普通存储、加密存储、历史版本和旧对象兼容规则。
- 修复 0 字节文件上传完成时服务端找不到临时分片文件而返回 500 的问题，后端会为合法的 0 字节上传创建空对象文件；该修复不迁移、不删除、不重写已有服务端数据。
- 上传会话增加已完成版本的幂等识别：当客户端因旧失败队列重复创建、查询或完成同一 `version_id` 时，后端直接返回已完成状态，避免旧 pending 会话临时文件缺失造成重复“服务器处理失败”。
- 客户端上传重试不再复用“服务端显示 pending 但已收满字节”的旧上传会话，遇到这类陈旧会话会新建会话重传，避免旧临时分片缺失时反复失败。
- Android/Flutter 本地目录扫描和同步主页队列修剪跳过 `.drive_sync` 第三方同步控制目录，避免把群晖 Drive 等应用生成的隐藏索引文件加入 VaultSync 上传队列；升级后已存在的 `.drive_sync` 失败任务会从本地队列中自动移除，不影响普通用户文件和服务端已上传对象。
- 同步目录新增“服务器端加密存储”开关：旧目录默认保持加密，新建普通文件夹可选择普通存储；加密文件落到 `data/objects/{user_id}/encrypted/`，普通文件落到 `data/objects/{user_id}/plain/`，相册备份仍默认强制加密。本次为非破坏性新增字段，不迁移、不删除、不重写已有密文文件或 SQLite 数据。
- 新增 `docs/notes/server-storage-layout.md`，记录 NAS 与容器内数据目录映射、加密/普通对象目录和旧对象兼容规则。
- 新增根目录 `.dockerignore`，避免后端镜像构建时把 Flutter 构建产物、前端 `node_modules` 和本地配置打入 Docker 上下文。
- 优化客户端上传过程中的本地进度保存频率，分片上传时减少整条上传队列反复写入，降低大量文件上传时的界面卡顿。
- 优化官网首页中文主标题排版，将“把你的文件云 / 重新放回自己手里”拆成稳定双行并收敛字号，避免桌面端标题挤压错乱。
- Flutter 客户端接入设备信息读取，注册设备和当前设备展示名优先使用用户可理解的名称，例如 `HUAWEI NOH-AN00`，内部同步归属仍继续使用后端生成的 `device_id`。本次不迁移或删除已有设备、同步目录、上传队列和服务端数据。
- 优化同步主页目录卡片：目录名称固定单行省略，设备归属和状态标签移到副标题区域，避免移动端被右侧按钮挤压后显示混乱。
- 只读的其他设备目录不再展示“扫描此目录”“上传此目录”“重试失败任务”等本机操作，只保留查看说明，避免用户误点。本次仅调整客户端界面，不修改数据库、服务端数据、客户端绑定或上传队列。
- 同步主页增加设备归属区分：当前设备目录可扫描、上传、管理和删除服务器备份；其他设备目录标记为只读，只展示服务器备份和状态，避免点击同步时报“同步目录不属于当前设备”。
- 同步目录接口新增 `device_name` 返回字段，客户端优先展示设备名称，缺失时显示设备短 ID，方便区分多设备同步目录。
- 文件树展开改为分批渲染，文件很多时先显示前 150 项，并提供“继续显示更多”，减少一次性展开大量文件导致的卡顿。
- “全部上传”和自动上传限定为当前设备的同步目录，避免本地残留的其他设备任务参与当前设备同步。本次不变更数据库结构，不清理服务端数据或客户端上传队列。
- 对齐 Flutter 客户端已调用但后端缺失的接口：新增 `POST /api/v1/auth/refresh`、`GET /api/v1/upload-sessions/{sessionID}`、`PATCH /api/v1/sync-roots/{syncRootID}`、`DELETE /api/v1/sync-roots/{syncRootID}`，修复上传、断点续传、同步目录管理可能返回纯文本 404 的问题。
- 新增 `/api/v1/*` JSON 兜底：未知 API 或请求方法不支持时统一返回中文 JSON envelope，避免客户端再提示“服务器返回了无法解析的响应”。
- 将上传、同步目录、设备、下载、删除和通用内部错误等高频后端错误提示改为中文。
- 删除同步目录时默认只删除目录绑定，不物理删除密文文件；仅当请求明确 `delete_remote=true` 时写入远端删除墓碑。本次不新增数据库字段，不清空服务端数据目录或 SQLite 数据库。
- 修复手机端同步主页加载服务器备份列表时，后端缺少 `GET /api/v1/sync-roots/{syncRootID}/remote-objects` 导致返回纯文本 `404 page not found`，客户端提示“服务器返回了无法解析的响应”的问题。
- 新增服务器备份对象分页查询接口，返回统一 JSON envelope，并只列出当前用户、当前同步目录下未被删除墓碑覆盖的最新文件版本。
- 将认证失败时的缺少 token、token 无效提示改为中文，避免客户端直接展示英文后端消息。
- 本次变更不涉及数据库结构迁移，不清理服务端 `data/`、SQLite 数据库、密文文件、下载目录或客户端本地同步记录。

## 2026-07-08

- Android “新增同步目录”弹窗参考群晖 Drive 的本地路径选择方式重构：将“同步‘下载’文件夹”和“同步指定文件夹”拆成清晰选项，并保留文件访问授权入口，避免用户被系统目录选择器的灰色按钮困住。

## 2026-07-07

- Android “新增同步目录”弹窗新增“使用下载目录路径”按钮，解决系统目录选择器在已授权后仍禁止选择“下载”根目录的问题；该能力仍保留在原同步目录流程内，不新增独立下载备份入口。
- 现有“新增同步目录”弹窗增加 Android 文件访问授权引导，不新增下载目录入口；用户遇到下载目录等受限位置时可跳转系统页手动开启所有文件访问权限。
- 增加升级数据安全规则：改动、升级和部署前必须评估已有数据影响，默认禁止丢失服务端数据、客户端同步记录和加密密钥。
- 优化 Android 目录选择提示，说明系统可能禁止直接选择“下载”根目录、存储根目录或 `Android/data`，建议用户选择自建子文件夹如 `Download/VaultSync`。
- 修复 Android release APK 缺少 `INTERNET` 权限导致无法访问 `https://files.ligson.xyz` 的问题，并新增 manifest 回归测试；已重新构建并上传带联网权限的最新 APK。
- Flutter 客户端统一包装网络连接失败，登录、注册、设备注册等请求无法连接后端时显示明确中文提示，不再兜底为“操作失败，请稍后重试”。
- 登录流程新增分阶段错误提示，区分“登录成功后注册设备失败”“生成本地加密密钥失败”“保存本地登录状态失败”，方便定位手机端登录卡点。
- 修复普通用户重复注册时后端返回 `500 internal server error` 的问题，改为统一 JSON envelope 返回中文提示“该邮箱已注册，请直接登录或更换邮箱”。
- 发布并部署后端镜像 `ligson/vaultsync-be:20260707100101-2e5a606-dupemail` 到 nas-proxy，线上注册接口已验证重复邮箱返回可读中文错误。
- 将完整 Flutter 客户端从功能工作区合并回主目录，恢复同步中心、目录绑定、本地清理策略、相册备份、退出登录、服务端备份浏览与删除等客户端能力。
- 重新生成 Android release 签名 APK，release 默认后端地址保持为 `https://files.ligson.xyz`，并上传到 nas-proxy 的最新 Android 下载位置。
- 修正前端 workspace 忽略规则，避免 `node_modules`、`tsconfig.tsbuildinfo` 和类型检查生成物污染提交。

## 2026-07-06

- 同步 nas-proxy 真实后端配置，补齐 `app.admin` 配置段，并沉淀“配置结构变更必须同步远程 config.yaml”的私有部署规则。
- 生成 Android release 签名 APK，并将后端与前端镜像发布部署到 nas-proxy；官网、管理后台、健康检查和 Android 下载链接已完成线上验证。
- 下载管理补齐安装包文件大小、平台文件类型校验、上传进度、复制下载链接和删除旧包能力；后端同步新增大小字段与删除接口。
- 修复管理后台注册/登录表单缺少字段绑定导致点击提交无反馈的问题，并补充中文校验提示。
- 修复管理后台概览接口空审计事件返回 `null` 导致首页渲染失败的问题，统一空列表为 `[]` 并补充前端兜底。
- 管理后台拆分“用户管理”和“配额管理”：用户管理支持新增用户、锁定/启用登录、重置密码；配额管理专注查看已用空间与调整限额。
- 后端新增管理员创建普通用户与重置用户密码接口，并保持统一 JSON envelope。
- 修复管理后台顶部栏压住页面标题的布局问题，调整为稳定的侧边栏、顶部栏和内容区结构。
- 优化管理后台顶部栏标题排版，覆盖 Ant Design Header 默认行高导致的文字上下错位问题。
- 管理后台右上角操作区改为统一工具栏样式，并复用客户端 VaultSync 应用图标作为官网与后台品牌标识。
- 官网和管理后台新增统一的 `shortcut icon`、`favicon` 与 Apple touch icon，全部使用 VaultSync 应用图标。
- 管理后台新增“系统状态”页面，展示后端状态、监听地址、数据目录、数据库、下载目录和容量占用。
- 管理后台新增“审计日志”页面，展示管理员创建用户、重置密码、更新配置、上传安装包等关键操作记录。
- 后端新增管理员审计日志列表和系统状态接口，并在关键管理员操作成功后写入审计日志。
- 管理后台右上角显示真实管理员邮箱，支持修改当前管理员密码并在成功后重新登录。
- 前端 API 错误新增 HTTP 状态信息，管理后台遇到 401/403 会清理 token、提示登录过期并跳转登录页。
- 下载管理新增真实安装包上传能力，上传后保存到后端下载目录、更新 latest 元数据，并通过 `/downloads/` 公开下载。
- 新增后端公开健康检查接口 `GET /api/v1/health`，返回统一 JSON envelope，便于部署后验证服务与反向代理链路。
- Flutter 客户端 release 构建默认后端地址改为 `https://files.ligson.xyz`，调试构建仍默认使用 `http://127.0.0.1:8080`，显式配置继续最高优先级，并将统一配置接入应用入口与登录页。
- 新增 `docs/private/` 忽略规则，用于保存本地私有部署记录；私有服务器信息和真实部署配置不得提交。
- 完成一次单机 NAS 部署验证：后端使用 Docker Compose 单容器运行，nginx 通过域名转发到后端健康检查接口。
- 后端部署方式切换为镜像仓库模式，镜像发布到 `ligson/vaultsync-be`，NAS Compose 直接使用远程镜像，不再在部署目录保留源码或 runtime 构建目录。
- 新增后端镜像发布规范、`docker/release.Dockerfile` 与 Makefile 发布目标，固定 `linux/amd64` 镜像构建和推送流程。
- 初始化 `vaultsync-fe` 前端源码工程，包含官网首页 `portal`、管理后台 `admin`、共享 API client、共享主题包和前端容器 nginx 配置。
- 新增管理后台真实 API：管理员注册/登录、管理员鉴权、概览、用户、系统配置和下载列表接口；后台页面接入真实接口并增加登录/注册页。
- 补齐管理后台写接口：支持修改用户状态和限额、保存系统配置、更新各平台最新下载信息；前端按钮已接入对应 API。
- 后端配置改为 `config.yaml` 文件方式，支持 `-config` 指定配置路径，不再要求用户通过环境变量配置服务端口和存储路径。
- 后端配置文件新增 `app` 根节点，配置按 `app.server`、`app.storage`、`app.security` 分类，避免顶层配置项持续膨胀。
- 新增 `vaultsync-be/config.example.yaml` 与 `vaultsync-be/docker/config.example.yaml`，示例配置包含中文注释；真实 `config.yaml` 已加入忽略规则。
- 明确后端默认存储规则：`app.storage.data_dir` 默认 `./data`，`app.storage.database_path` 默认 `{app.storage.data_dir}/vaultsync.db`，`app.security.token_secret` 必须显式填写。
- 更新 README、Docker Compose、Makefile 和后端配置规范文档，统一说明配置文件启动方式。

## 2026-06-26

- 重构仓库目录：后端代码迁移到 `vaultsync-be/`，预留 `vaultsync-app/` 和 `vaultsync-fe/`，并同步更新根 README、规则和后端入口文件。
- 完成变更列表分页：`GET /api/v1/changes` 支持 `limit`，响应新增 `has_more`，服务端使用 `LIMIT limit+1` 探测是否还有下一页。
- 对齐同步协议、决策记录和后端 MVP 记忆，修正设备维度游标与远端删除墓碑的当前支持状态。
- 完成删除墓碑与变更类型：新增 `file_tombstones`、`sync_events`、`DELETE /api/v1/objects/{object_id}`，变更列表返回 `upsert/delete` 并使用统一事件游标。
- 新增删除墓碑与变更类型实现计划，准备让变更列表同时表达 `upsert` 和 `delete`。
- 完成设备维度变更游标：`GET /api/v1/changes` 支持可选 `device_id`，`sync_cursors` 升级为 `(user_id, device_id)` 复合主键，并保留 `__legacy__` 兼容键。
- 新增设备维度变更游标实现计划，准备将 `sync_cursors` 从用户维度升级为用户+设备维度并保持旧调用兼容。
- 新增 VaultSync 客户端本地清理策略 V1，明确 `keep`、`delete`、`archive` 行为、安全触发条件、移动端相册权限、失败重试和后端边界。
- 新增客户端本地清理策略 V1 计划，开始沉淀 `keep`、`delete`、`archive` 的安全触发条件、失败重试和移动端权限边界。
- 新增 VaultSync 同步协议 V1，明确设备初始化、本地扫描、上传、变更拉取、下载、冲突副本、本地清理触发点和当前后端支持边界。
- 新增同步协议 V1 计划，开始沉淀文件级同步、变更游标、冲突副本和本地清理触发点。
- 新增 VaultSync 加密格式规范 V1，明确客户端密钥层级、内容密文对象格式、加密元数据格式和后端不解析密文的边界。
- 新增加密格式规范计划，开始沉淀客户端加密格式、密钥层级和后端密文处理边界。
- 完成 API 错误响应规范化：认证中间件和现有 API handler 统一返回 JSON 错误体，并补充认证、无效请求、上传和下载错误测试。
- 新增 API 错误响应规范与实现计划，下一步统一后端错误体为 JSON，方便客户端处理。
- 完成后端隔离与上传链路硬化：新增跨用户设备/同步目录/下载隔离测试，补充上传会话归属、状态、大小和完整性校验。
- 新增后端隔离与上传链路硬化设计、实现计划，下一阶段聚焦设备/同步目录归属校验、上传状态与大小完整性校验。
- 收口后端 MVP 的计划文件结构：补充独立的同步目录测试、下载服务拆分、聚合测试、更完整的 README / notes，并让 `sync_cursors` 实际参与变更游标持久化。
- 补齐后端 MVP 收尾：HTTP 服务实际监听启动、Docker 单机部署文件、README 与后端 MVP 记忆文档。

## 2026-06-27

- 统一后端对外 JSON 响应为 `success/message/httpCode/data` envelope，新增共享响应包 `internal/httpapi/response`，并让认证中间件复用同一结构返回 401。
- 新增轻量领域错误类型，服务层可返回稳定错误码，handler 统一映射为 JSON envelope；跨用户下载或版本不存在返回 `404 not_found`，不再暴露底层 SQL 错误。
- 将后端集成测试迁移到 `vaultsync-be/tests/integration/`，确保 `cd vaultsync-be && go test ./...` 覆盖 API 主流程。
- 新增客户端 API 对接约定文档，说明 Flutter 客户端如何处理 envelope、错误码和二进制下载例外。
- 更新所有主要 API handler 的成功响应为统一 envelope，下载接口继续保持二进制输出。
- 改造测试辅助函数以解析统一 envelope，补齐认证、注册、同步目录、上传和变更列表相关测试适配。
- 将 API 响应规范文档改为统一 JSON envelope 约定，并把这条规则写入仓库规则文件。

## 2026-06-25

- 新增变更拉取与密文下载接口，当前用 `file_versions` 的顺序行号充当最小游标。
- 新增上传会话、分片追加、完成落盘与文件版本记录的最小闭环。
- 新增设备注册与同步目录新增、查询接口，并将相关接口接入 Bearer Token 鉴权。
- 新增注册与登录认证链路，包括 bcrypt 密码哈希、HMAC Bearer Token、认证路由和集成测试辅助工具。
- 对齐 SQLite 初始化表结构与后端 MVP 实现计划，补充核心表和关键列的 schema 测试。
- 新增 SQLite 初始化与迁移层，启用 WAL，并创建后端 MVP 核心元数据表。
- 收紧 Go 后端工程骨架基线：降低 Go 版本目标、补充配置测试，并忽略本地构建产物。
- 初始化 Go 后端工程骨架，新增配置加载、应用装配、服务入口和基础测试。
- 新增 `.gitignore`，忽略 `.worktrees/` 隔离工作区目录。
- 新增 Go + SQLite 后端 MVP 实现计划文档，路径为 `docs/superpowers/plans/2026-06-25-go-sqlite-backend-mvp.md`。
- 补充规则：实现计划统一放在 `docs/superpowers/plans/`。
- 将根目录 `README.md` 中文化。
- 将项目文档规则改为尽可能使用中文编写，技术名词和代码标识可保留英文。
- 将现有规则、说明和决策文档中的英文说明改为中文。
- 新增 `AGENTS.md` 仓库规则。
- 新增 `docs/` 文档结构。
- 新增第一版 VaultSync 设计稿。
- 新增用于沉淀决策和项目记忆的 `docs/notes/`。
